#include <iostream>
#include <vector>

std::vector<std::string> set_String;


int main(){
    int n;
    std::cin>>n;
    std::string str;
    for(int i = 0;i < n;++i){
        std::cin>>str;
        set_String.push_back(str);
    }
    std::string command;
    std::cin>>command;
    while(command != "over"){
        if(command == )
    }


    return 0;
}