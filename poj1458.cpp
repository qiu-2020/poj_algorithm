#include <iostream>
#include <cstring>

char a[1010],b[1010],m,n;
int ans[1010][1010];

// maxl[i,j] = std::max(maxl[i - 1],)

void count_max_common(){
    for(int i = 0;i < m;++i){
        for(int j = 0;j < n;++j){
            
        }
    }
}

int main(){
    while(std::cin>>a>>b && a && b){
        m = strlen(a),n = strlen(b);
        count_max_common();
    }
    return 0;
}